@extends('layouts.app')
@section('title', 'Jobdesk')
@section('content')
  <div class="page-header"><div><h1 class="page-title">Jobdesk</h1><p class="page-subtitle">Daftar jobdesk berdasarkan divisi dan jabatan.</p></div><a class="btn btn-primary btn-sm" href="{{ route('hr.job-descriptions.create') }}">Tambah Jobdesk</a></div>
  @include('hr.partials.alerts')
  <div class="card"><div class="card-header"><form class="filter-row"><select class="form-select" name="division_id"><option value="">Semua Divisi</option>@foreach($divisions as $division)<option value="{{ $division->id }}" @selected(request('division_id')==$division->id)>{{ $division->name }}</option>@endforeach</select><select class="form-select" name="position_id"><option value="">Semua Jabatan</option>@foreach($positions as $position)<option value="{{ $position->id }}" @selected(request('position_id')==$position->id)>{{ $position->name }}</option>@endforeach</select><button class="btn btn-outline">Filter</button></form></div><div class="table-responsive"><table class="data-table"><thead><tr><th>Judul</th><th>Divisi</th><th>Jabatan</th><th>Supervisor</th><th>Status</th><th class="th-aksi">Aksi</th></tr></thead><tbody>@forelse($jobDescriptions as $job)<tr><td>{{ $job->title }}</td><td>{{ $job->division?->name ?? '-' }}</td><td>{{ $job->position?->name ?? '-' }}</td><td>{{ $job->directSupervisor?->nama ?? '-' }}</td><td><span class="status-badge status-{{ $job->status }}">{{ ucfirst($job->status) }}</span></td><td><div class="action-btns"><a class="btn-action btn-view" href="{{ route('hr.job-descriptions.show',$job) }}"><i class="bi bi-eye-fill"></i></a><a class="btn-action btn-edit" href="{{ route('hr.job-descriptions.edit',$job) }}"><i class="bi bi-pencil-fill"></i></a><form method="POST" action="{{ route('hr.job-descriptions.destroy',$job) }}" data-confirm-delete="Hapus jobdesk {{ $job->title }}?">@csrf @method('DELETE')<button class="btn-action btn-delete"><i class="bi bi-trash3-fill"></i></button></form></div></td></tr>@empty<tr><td colspan="6"><div class="empty-state"><div class="empty-title">Belum ada jobdesk</div></div></td></tr>@endforelse</tbody></table></div><div class="table-footer">{{ $jobDescriptions->links() }}</div></div>
@endsection
