@extends('layouts.app')
@section('title', 'Penggajian')
@section('content')
  <div class="page-header"><div><h1 class="page-title">Penggajian</h1><p class="page-subtitle">Kelola gaji, status pembayaran, dan slip sederhana.</p></div><a class="btn btn-primary btn-sm" href="{{ route('hr.salaries.create') }}">Tambah Gaji</a></div>
  @include('hr.partials.alerts')
  <div class="card"><div class="card-header"><form class="filter-row"><select class="form-select" name="employee_id"><option value="">Semua Karyawan</option>@foreach($employees as $employee)<option value="{{ $employee->id }}" @selected(request('employee_id')==$employee->id)>{{ $employee->nama }}</option>@endforeach</select><input class="form-control" type="month" name="salary_period" value="{{ request('salary_period') }}"><select class="form-select" name="payment_status"><option value="">Semua Status</option>@foreach($statuses as $status)<option value="{{ $status }}" @selected(request('payment_status')===$status)>{{ str_replace('_',' ',ucfirst($status)) }}</option>@endforeach</select><button class="btn btn-outline">Filter</button></form></div><div class="table-responsive"><table class="data-table"><thead><tr><th>Karyawan</th><th>Periode</th><th>Total</th><th>Status</th><th>Tanggal Bayar</th><th class="th-aksi">Aksi</th></tr></thead><tbody>@forelse($salaries as $salary)<tr><td>{{ $salary->employee?->nama }}</td><td>{{ $salary->salary_period }}</td><td>Rp {{ number_format($salary->total_salary,0,',','.') }}</td><td><span class="status-badge status-{{ $salary->payment_status }}">{{ str_replace('_',' ',ucfirst($salary->payment_status)) }}</span></td><td>{{ $salary->payment_date?->format('d M Y') ?? '-' }}</td><td><div class="action-btns"><a class="btn-action btn-view" href="{{ route('hr.salaries.show',$salary) }}"><i class="bi bi-eye-fill"></i></a><a class="btn-action btn-edit" href="{{ route('hr.salaries.edit',$salary) }}"><i class="bi bi-pencil-fill"></i></a>@if($salary->payment_status !== 'sudah_dibayar')<form method="POST" action="{{ route('hr.salaries.mark-paid',$salary) }}">@csrf @method('PATCH')<button class="btn-action btn-view" title="Tandai Dibayar"><i class="bi bi-check2-circle"></i></button></form>@endif<form method="POST" action="{{ route('hr.salaries.destroy',$salary) }}" data-confirm-delete="Hapus data gaji?">@csrf @method('DELETE')<button class="btn-action btn-delete"><i class="bi bi-trash3-fill"></i></button></form></div></td></tr>@empty<tr><td colspan="6"><div class="empty-state"><div class="empty-title">Belum ada data gaji</div></div></td></tr>@endforelse</tbody></table></div>@include('hr.partials.pagination', ['paginator' => $salaries])</div>
@endsection
