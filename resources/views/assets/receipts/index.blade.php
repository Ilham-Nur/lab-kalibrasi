@extends('layouts.app')
@section('title','Penerimaan Barang')
@section('content')
  <div class="page-header"><div><h1 class="page-title">Penerimaan Barang</h1><p class="page-subtitle">Penerimaan item dari pengadaan yang sudah approved/purchasing.</p></div><a class="btn btn-primary btn-sm" href="{{ route('assets.receipts.create') }}"><i class="bi bi-plus-lg"></i> Buat Penerimaan</a></div>
  @include('hr.partials.alerts')
  @include('assets.procurements._tabs')
  <div class="card"><div class="card-header"><form class="filter-row"><select class="form-select" name="status"><option value="">Semua Status</option>@foreach($statuses as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ str_replace('_',' ',ucfirst($status)) }}</option>@endforeach</select><button class="btn btn-outline">Filter</button></form></div><div class="table-responsive"><table class="data-table"><thead><tr><th>Nomor</th><th>Procurement</th><th>Tanggal</th><th>Supplier</th><th>Status</th><th class="th-aksi">Aksi</th></tr></thead><tbody>@forelse($receipts as $receipt)<tr><td><strong>{{ $receipt->receipt_number }}</strong></td><td>{{ $receipt->procurement?->procurement_number }}</td><td>{{ $receipt->received_date?->format('d M Y') }}</td><td>{{ $receipt->supplier_name ?: '-' }}</td><td><span class="status-badge status-{{ $receipt->status }}">{{ str_replace('_',' ',ucfirst($receipt->status)) }}</span></td><td><div class="action-btns"><a class="btn-action btn-view" href="{{ route('assets.receipts.show',$receipt) }}"><i class="bi bi-eye-fill"></i></a><a class="btn-action btn-edit" href="{{ route('assets.receipts.edit',$receipt) }}"><i class="bi bi-pencil-fill"></i></a><form method="POST" action="{{ route('assets.receipts.destroy',$receipt) }}" data-confirm-delete="Hapus penerimaan {{ $receipt->receipt_number }}?">@csrf @method('DELETE')<button class="btn-action btn-delete"><i class="bi bi-trash3-fill"></i></button></form></div></td></tr>@empty<tr><td colspan="6"><div class="empty-state"><div class="empty-title">Belum ada penerimaan</div></div></td></tr>@endforelse</tbody></table></div>@include('hr.partials.pagination',['paginator'=>$receipts])</div>
@endsection
