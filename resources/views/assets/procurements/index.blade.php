@extends('layouts.app')
@section('title','Pengajuan Pengadaan')
@section('content')
  <div class="page-header"><div><h1 class="page-title">Pengajuan Pengadaan</h1><p class="page-subtitle">Daftar permintaan pengadaan aset dan status approval.</p></div><a class="btn btn-primary btn-sm" href="{{ route('assets.procurements.create') }}"><i class="bi bi-plus-lg"></i> Buat Pengadaan</a></div>
  @include('hr.partials.alerts')
  @include('assets.procurements._tabs')
  <div class="card"><div class="card-header"><form class="filter-row"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Cari nomor / department / supplier"><select class="form-select" name="status"><option value="">Semua Status</option>@foreach($statuses as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ str_replace('_',' ',ucfirst($status)) }}</option>@endforeach</select><button class="btn btn-outline">Filter</button></form></div><div class="table-responsive"><table class="data-table"><thead><tr><th>Nomor</th><th>Tanggal</th><th>Supplier</th><th>Requested By</th><th>Status</th><th>Level</th><th>Total</th><th class="th-aksi">Aksi</th></tr></thead><tbody>@forelse($procurements as $procurement)<tr><td><strong>{{ $procurement->procurement_number }}</strong><div class="td-email-sub">{{ $procurement->department ?: '-' }}</div></td><td>{{ $procurement->request_date?->format('d M Y') }}</td><td>{{ $procurement->supplier?->name ?? '-' }}</td><td>{{ $procurement->requestedBy?->name ?? '-' }}</td><td><span class="status-badge status-{{ $procurement->status }}">{{ str_replace('_',' ',ucfirst($procurement->status)) }}</span></td><td>{{ $procurement->current_approval_level ?: '-' }}</td><td>Rp {{ number_format($procurement->total_estimated_cost,0,',','.') }}</td><td><div class="action-btns"><a class="btn-action btn-view" href="{{ route('assets.procurements.show',$procurement) }}"><i class="bi bi-eye-fill"></i></a>@if($procurement->status==='draft')<a class="btn-action btn-edit" href="{{ route('assets.procurements.edit',$procurement) }}"><i class="bi bi-pencil-fill"></i></a><form method="POST" action="{{ route('assets.procurements.submit',$procurement) }}">@csrf<button class="btn-action btn-view" title="Submit"><i class="bi bi-send-fill"></i></button></form>@endif<form method="POST" action="{{ route('assets.procurements.destroy',$procurement) }}" data-confirm-delete="Hapus pengadaan {{ $procurement->procurement_number }}?">@csrf @method('DELETE')<button class="btn-action btn-delete"><i class="bi bi-trash3-fill"></i></button></form></div></td></tr>@empty<tr><td colspan="8"><div class="empty-state"><div class="empty-title">Belum ada pengadaan</div></div></td></tr>@endforelse</tbody></table></div>@include('hr.partials.pagination',['paginator'=>$procurements])</div>
@endsection
