@extends('layouts.app')
@section('title','Detail Pemeriksaan')
@section('content')
  <div class="page-header"><div><h1 class="page-title">{{ $inspection->inspection_number }}</h1><p class="page-subtitle">{{ $inspection->asset?->asset_code }} - {{ $inspection->asset?->name }}</p></div><div class="action-btns"><a class="btn btn-outline btn-sm" href="{{ route('assets.inspections.index') }}"><i class="bi bi-arrow-left"></i> Kembali</a><a class="btn btn-primary btn-sm" href="{{ route('assets.inspections.edit',$inspection) }}">Edit</a></div></div>
  <div class="card"><div class="modal-body"><div class="detail-grid"><div class="detail-item"><div class="detail-label">Tanggal</div><div class="detail-value">{{ $inspection->inspection_date?->format('d M Y') }}</div></div><div class="detail-item"><div class="detail-label">Next</div><div class="detail-value">{{ $inspection->next_inspection_date?->format('d M Y') ?? '-' }}</div></div><div class="detail-item"><div class="detail-label">Petugas</div><div class="detail-value">{{ $inspection->inspectedBy?->name ?? '-' }}</div></div><div class="detail-item"><div class="detail-label">Result</div><div class="detail-value">{{ str_replace('_',' ',ucfirst($inspection->result)) }}</div></div><div class="detail-item"><div class="detail-label">Status</div><div class="detail-value">{{ str_replace('_',' ',ucfirst($inspection->status)) }}</div></div><div class="detail-item"><div class="detail-label">Catatan</div><div class="detail-value">{{ $inspection->notes ?: '-' }}</div></div></div></div></div>
  <div class="card"><div class="card-header"><h2 class="card-title">Checklist</h2></div><div class="table-responsive"><table class="data-table"><thead><tr><th>Checklist</th><th>Result</th><th>Notes</th></tr></thead><tbody>@forelse($inspection->items as $item)<tr><td>{{ $item->checklist_name }}</td><td>{{ str_replace('_',' ',ucfirst($item->result)) }}</td><td>{{ $item->notes ?: '-' }}</td></tr>@empty<tr><td colspan="3"><div class="empty-state"><div class="empty-title">Belum ada checklist</div></div></td></tr>@endforelse</tbody></table></div></div>
@endsection
