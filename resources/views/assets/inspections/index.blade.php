@extends('layouts.app')
@section('title','Pemeriksaan Berkala')
@section('content')
  <div class="page-header"><div><h1 class="page-title">Pemeriksaan Berkala</h1><p class="page-subtitle">Jadwal, hasil, dan checklist pemeriksaan aset.</p></div><a class="btn btn-primary btn-sm" href="{{ route('assets.inspections.create') }}"><i class="bi bi-plus-lg"></i> Buat Pemeriksaan</a></div>
  @include('hr.partials.alerts')
  <div class="card"><div class="card-header"><form class="filter-row"><select class="form-select" name="status"><option value="">Semua Status</option>@foreach($statuses as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ str_replace('_',' ',ucfirst($status)) }}</option>@endforeach</select><select class="form-select" name="result"><option value="">Semua Result</option>@foreach($results as $result)<option value="{{ $result }}" @selected(request('result')===$result)>{{ str_replace('_',' ',ucfirst($result)) }}</option>@endforeach</select><input class="form-control" type="date" name="date_from" value="{{ request('date_from') }}"><input class="form-control" type="date" name="date_to" value="{{ request('date_to') }}"><button class="btn btn-outline">Filter</button></form></div><div class="table-responsive"><table class="data-table"><thead><tr><th>Nomor</th><th>Aset</th><th>Tanggal</th><th>Next</th><th>Result</th><th>Status</th><th class="th-aksi">Aksi</th></tr></thead><tbody>@forelse($inspections as $inspection)<tr><td><strong>{{ $inspection->inspection_number }}</strong></td><td>{{ $inspection->asset?->asset_code }}<div class="td-email-sub">{{ $inspection->asset?->name }}</div></td><td>{{ $inspection->inspection_date?->format('d M Y') }}</td><td>{{ $inspection->next_inspection_date?->format('d M Y') ?? '-' }}</td><td>{{ str_replace('_',' ',ucfirst($inspection->result)) }}</td><td><span class="status-badge status-{{ $inspection->status }}">{{ str_replace('_',' ',ucfirst($inspection->status)) }}</span></td><td><div class="action-btns"><a class="btn-action btn-view" href="{{ route('assets.inspections.show',$inspection) }}"><i class="bi bi-eye-fill"></i></a><a class="btn-action btn-edit" href="{{ route('assets.inspections.edit',$inspection) }}"><i class="bi bi-pencil-fill"></i></a><form method="POST" action="{{ route('assets.inspections.destroy',$inspection) }}" data-confirm-delete="Hapus pemeriksaan?">@csrf @method('DELETE')<button class="btn-action btn-delete"><i class="bi bi-trash3-fill"></i></button></form></div></td></tr>@empty<tr><td colspan="7"><div class="empty-state"><div class="empty-title">Belum ada pemeriksaan</div></div></td></tr>@endforelse</tbody></table></div>@include('hr.partials.pagination',['paginator'=>$inspections])</div>
  <div class="card"><div class="card-header"><h2 class="card-title">Aset Overdue Pemeriksaan</h2></div><div class="table-responsive"><table class="data-table"><tbody>@forelse($overdueAssets as $asset)<tr><td>{{ $asset->asset_code }}</td><td>{{ $asset->name }}</td><td>{{ $asset->next_inspection_date?->format('d M Y') }}</td></tr>@empty<tr><td><div class="empty-state"><div class="empty-title">Tidak ada overdue</div></div></td></tr>@endforelse</tbody></table></div></div>
@endsection
